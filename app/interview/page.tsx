import InterviewGate from "./InterviewGate";

export const metadata = {
  title: "Interview — Pixie",
  description: "Start your Pixie interview session.",
};

export default function InterviewPage() {
  return <InterviewGate />;
}
