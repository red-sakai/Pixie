import InterviewSession from "./session";

export const metadata = {
  title: "Interview Session — Pixie",
  description: "Your Pixie interview session.",
};

export default function InterviewSessionPage() {
  return <InterviewSession />;
}
